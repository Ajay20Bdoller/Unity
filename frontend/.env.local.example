# Copy this file to .env.local and adjust as needed. Never commit .env.local.
# Base URL of the FastAPI backend. Must be a public (non-secret) value —
# anything prefixed NEXT_PUBLIC_ is bundled into client-side JS.
NEXT_PUBLIC_API_URL=http://localhost:8000
